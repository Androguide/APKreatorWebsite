// Generated by CoffeeScript 1.6.3
(function() {
  'use strict';
  angular.module('ngApkreator').controller('SignInCtrl', function($scope, $http) {
    var hoodie;
    hoodie = new Hoodie();
    $scope.user = {
      name: "",
      password: ""
    };
    $scope.signUserIn = function(isValid) {
      $('.ng-pristine').addClass('ng-invalid');
      if (isValid) {
        return $http.get('http://localhost:5000/is_confirmed/' + $scope.user.name).success(function(data) {
          console.log("is confirmed: ", data.confirmed);
          if (data.confirmed) {
            hoodie.account.signIn($scope.user.name, $scope.user.password);
            return vex.closeAll();
          } else {
            console.log("error", data.error, data.status);
            return vex.dialog.alert("Please confirm your account by clicking on the link that was sent to you by email");
          }
        }).error(function(data, status) {
          return console.log("error", data, status);
        });
      } else {
        return console.log("invalid form");
      }
    };
    hoodie.account.on('signin', function(user) {
      console.log("Signed-in: ", user);
      return vex.closeAll();
    });
    return hoodie.account.on('signout', function(user) {
      return console.log("Goodbye  ", user);
    });
  });

}).call(this);

/*
//@ sourceMappingURL=sign-in.map
*/
